{
  "name": "rag-chat-box-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "start": "npm --prefix frontend start"
  },
  "dependencies": {
    "next": "16.2.4"
  }
}
