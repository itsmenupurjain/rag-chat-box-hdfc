services:
  - type: web
    name: hdfc-rag-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHONPATH
        value: .
